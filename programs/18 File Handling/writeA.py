#write function by using append 'a'

with open('E:\Python\PY lab\programs\18 File Handling','a') as file :
    file.write("\nUsing this 'a' the data is stored in the file but already present data is stay as it is in the file")